import { Book } from "./Book";

export class BookShelf {
    _books: Book[] = [];

    addBookToCataog(newBook: Book) {
        // add code here to catalog book
    }

    removeBookFromCatalog(oldBook: Book) {
        // add code here to remove book
    }
}