// generic class
export class Shelf<T> {

    _items: T[] = [];

    addBookToCataog(newItem: T) {
        // add code here to catalog item
    }

    removeBookFromCatalog(oldItem: T) {
        // add code here to remove item
    }
}